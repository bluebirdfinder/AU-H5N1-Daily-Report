# HPAI 疫情監控程式時間過濾與防重複發送優化計劃

此計劃旨在優化 `monitor.py` 的日期過濾與狀態記錄邏輯，以完全符合使用者的最新需求：
1. **最近一週**：後續運行中，新聞與疫情通報的篩選範圍嚴格限制在最近一週（7天）。
2. **第一次放寬**：若為第一次運行（狀態檔中對應的已通知列表為空），時間過濾放寬到今年（2026年），以便使用者在測試中能看到今年的數據。
3. **防重複與補發**：第一次運行發送最新一筆後，**將所有今年以來的其他舊項目一次性標記為「已處理」**。如此一來，後續運行時，絕對不會再一筆一筆補發過去的歷史舊帳，徹底解決重複發送的問題。
4. **澳洲優先官方網域與關鍵字擴展**：
   - **關鍵字**：擴展 RSS 搜尋關鍵字，完整納入主要 H 抗原與 N 抗原組合（如 H5N1, H7N9, H5N2, H5N8, H7N3, H7N8 等）。
   - **優先權**：若有多個新消息，優先挑選網址包含官方網域（`.gov.au`、`wildlifehealthaustralia.com.au`）的公告；若無，才挑選主流媒體（如 ABC News）報導。

---

## 關鍵修改設計

1. **移除舊輔助函數**：
   - 移除 `is_new_nz_newsletter`
   - 移除 `is_new_wahis_report`
   （這兩個函數的邏輯被直接整合到 `run()` 內部的各模組新篩選流程中，使結構更簡潔、邏輯更嚴密）。

2. **優化 USDA 儀表板變動篩選**：
   - 判斷是否為首次執行（`status.get("usda_latest_date")` 為空）。
   - 解析 `latest_detection_date`，計算其與當前時間的間隔。
   - 僅在「首次執行」或「最新確診日期在 7 天內」且「數據有變動」時，才發送 Telegram 截圖與分析；否則只更新 status.json。

3. **重構紐西蘭 MPI 電子報篩選**：
   - 篩選條件：首次運行（狀態為空）放寬為 `dt.year == 2026`；後續運行限制為 `(datetime.now() - dt).days <= 7`。
   - 將所有符合時間條件的電子報項目寫入狀態檔。

4. **重構澳洲新聞 RSS 篩選**：
   - 篩選條件：首次運行放寬至 2026 年；後續運行限制為 `(datetime.now() - dt).days <= 7`。
   - 排列順序：若有多個新聞，按 `Priority Domains` 優先排序。
   - 發送邏輯：發送最新的一筆，其餘所有符合時間條件的新聞連結一次性標記已通知。

5. **重構 WOAH WAHIS 疫情通報篩選**：
   - 篩選條件：首次運行放寬至 2026 年；後續運行限制為 7 天內。
   - 發送邏輯：發送最新的一筆，其餘項目直接標記已通知。

6. **引入 Gemini API 429 防限流與延遲機制**：
   - 封裝 `call_gemini_api`：遇到 429 重試，等待 30 秒；每次呼叫成功後，強迫暫停 15 秒以平滑 API 使用頻率。

---

## 驗證計劃

### 自動測試
- 刪除 `status.json` 後在本地執行 `python monitor.py`，確認是否僅發送 2026 年最新的一筆訊息。
- 檢查 `status.json` 中是否已被寫入所有 2026 歷史通報記錄。
- 再次執行 `python monitor.py`，確認是否所有歷史通報均已跳過，且 Telegram 沒有重覆收到訊息。
