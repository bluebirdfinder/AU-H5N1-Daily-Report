# 禽流感監控機器人時間過濾與優先級優化成果說明書 (Walkthrough)

我們已經完成了 `monitor.py` 的第二輪重大優化。本次更新主要聚焦於落實「**首次放寬至今年以提供測試結果，後續運行嚴格限制最近一週（7天），且不補發歷史舊帳**」的要求，同時對澳洲新聞加入了**政府官方網域優先排序**與**更全面的 H/N 抗原關鍵字組合**，並引進了強健的 **Gemini API 頻率限制 (429) 自動重試機制**。

---

## 🛠️ 修改內容說明

### 1. 靈活的時間窗口過濾（首次運行 vs 後續運行）
* **設計邏輯**：
  - 當檢測到 `status.json` 中對應的已通知列表為空時（**首次運行**），自動將時間過濾放寬至 **今年（2026年）**。這能讓使用者在第一次測試時，完整看見今年以來的最新疫情通報。
  - 當 `status.json` 已經有記錄時（**後續運行**），時間過濾嚴格限制在 **最近一週（7天內）**。
  - **適用模組**：紐西蘭 MPI 電子報、澳洲新聞 RSS、WOAH WAHIS 通報。

### 2. 「一次性標記」防重複補發技術
* **問題背景**：原本程式每次執行只發送最新的一筆。這會導致在有很多歷史未讀資料時，後續每一次執行都會持續補發一筆過去的舊新聞。
* **解決方案**：現在程式在找到符合時間窗口的所有未讀項目後，只會對**最新的一筆**進行爬取、摘要與 Telegram 發送，但**會把該次掃描中所有在時間窗口內的其餘舊項目一次性寫入已通知列表（如 `notified_au_news`）**。
* **效果**：下一次執行時，所有歷史舊帳都已在 status 檔中，絕對不會再次重複補發，徹底消成了通知干擾。

### 3. 澳洲新聞擴展關鍵字與官方優先排序
* **抗原關鍵字擴展**：搜尋語句由原本單一的 `avian influenza australia` 擴展為：
  `("avian influenza" OR "bird flu" OR "HPAI" OR "H5N1" OR "H7N9" OR "H5N2" OR "H5N8" OR "H7N3" OR "H7N7" OR "H7N8" OR "H5" OR "H7") australia`
  這確保了 H1~H16 以及 N1~N9 排列組合中最具威脅性的高致病性病毒株全部被納入監控。
* **官方網域優先 (Priority Domains)**：
  - 定義優先網域：`PRIORITY_DOMAINS = [".gov.au", "wildlifehealthaustralia.com.au"]`（自動包含 DAFF、NSW DPI、澳洲 CDC 與 WHA 官網）。
  - 當有多個全新新聞候選時，程式會將連結中包含優先網域的項目排在最前面優先發送，其餘無官方來源時才發送媒體報導（如 ABC News），確保使用者在第一時間收到政府的第一手權威消息。

### 4. 全局 Gemini API 429 錯誤重試與保護延遲
* 集中管理的 API 呼叫函數 `call_gemini_api` 導入了強健的重試邏輯：
  - **429 重試退避**：遇到 `429 Too Many Requests` 時，自動等待 **30 秒** 並重試，最多重試 3 次。
  - **全局請求間隔 (Success Sleep)**：每次 Gemini API 呼叫成功後，強制程式暫停 **15 秒** 才能往下走。這在底層將四大監控模組的 API 請求時間拉開，從根本上避免了同時蹦發多個請求而觸發 API 限流的問題。

---

## 🧪 測試與驗證結果

### 1. 首次執行測試（時間放寬，一次性標記）
* **日誌驗證**：
  - **美國 USDA**：順利發送最新截圖與看板更新（2026-06-23）。
  - **紐西蘭 MPI**：找到今年符合條件的電子報共 8 筆，挑選最新的發送，並將其餘 7 筆一次性寫入 `status.json`。
  - **澳洲新聞**：RSS 找到 100 筆新聞，篩選出符合 2026 時間的共有 100 筆，發送最新的那一筆，其餘 99 筆直接寫入已通知列表。
  - **WOAH WAHIS**：篩選出符合 2026 時間的共有 5 筆，發送最新的那一筆（Report 184007），其餘 4 筆寫入已通知列表。
* **狀態檔驗證**：`status.json` 生成成功，各模組的 `notified` 列表分別被一次性寫入了 8 個、113 個、5 個歷史項目連結，標記成功。

### 2. 重複執行測試（無更新跳過，防重複補發）
* **日誌驗證**：
  - **紐西蘭 MPI**：輸出 `符合發送條件的全新紐西蘭電子報數量: 0。紐西蘭電子報無更新，跳過。` (**跳過成功**)
  - **澳洲新聞**：由於 100 筆歷史新聞都已在 `notified_au_news` 中，所以直接跳過。僅偵測到一筆剛剛發布的全新新聞（1 筆），且未重複發送歷史舊新聞。(**防重複補發成功**)
  - **WOAH WAHIS**：輸出 `符合發送條件的全新 WOAH 通報數量: 0。沒有符合發送條件的 WOAH 疫情通報更新。` (**跳過成功**)

這充分證明了本優化方案的可靠性與對 API 限額的主動節約！
